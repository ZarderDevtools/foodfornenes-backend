## API Versioning
Backend API currently at **v1** (frozen).

All breaking changes will be introduced in future versions (e.g., `/api/v2/`).
The OpenAPI spec for v1 is available at `docs/openapi/openapi_v1.json`.